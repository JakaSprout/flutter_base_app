/// Header data entity (domain layer).
class HeaderData {
  /// Creates a new instance of [HeaderData].
  const HeaderData({required this.notificationCount});

  /// Number of unread notifications
  final int notificationCount;
}
