/// Pond entity (domain layer).
class PondEntity {
  /// Creates a new instance of [PondEntity].
  const PondEntity({required this.id, required this.name});

  /// Pond ID
  final String id;

  /// Pond name
  final String name;
}
